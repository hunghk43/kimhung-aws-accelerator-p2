# Giá trị thực tế cho variables (file này KHÔNG commit nếu chứa secrets)
# File này override default values trong variables.tf

aws_region   = "ap-southeast-1"
project_name = "accelerator"
env          = "dev"
